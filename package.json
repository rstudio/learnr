{
  "name": "learnr",
  "private": true,
  "engines": {
    "node": ">= 14"
  },
  "scripts": {
    "build": "npm run build:tutorial-js && npm run build:tutorial-i18n && npm run build:tutorial-format && npm run build:esbuild",
    "build:tutorial-js": "babel src-js/tutorial -d inst/lib/tutorial",
    "build:tutorial-i18n": "babel src-js/i18n -d inst/lib/i18n",
    "build:tutorial-format": "babel src-js/format -d inst/rmarkdown/templates/tutorial/resources",
    "build:esbuild": "node src-js/build.js",
    "lint": "standard --fix src-js"
  },
  "devDependencies": {
    "@babel/cli": "^7.16.8",
    "@babel/core": "^7.16.12",
    "@babel/preset-env": "^7.16.11",
    "browserslist": "^4.19.1",
    "core-js": "^3.21.0",
    "esbuild": "^0.14.18",
    "standard": "^16.0.4"
  }
}
