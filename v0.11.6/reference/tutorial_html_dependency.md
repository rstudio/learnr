# Tutorial HTML dependency

HTML dependency for core tutorial JS and CSS. This should be included as
a dependency for custom tutorial formats that wish to ensure that that
`tutorial.js` and `tutorial.css` are loaded prior their own scripts and
stylesheets.

## Usage

``` r
tutorial_html_dependency()
```

## Value

learnr's HTML dependencies
