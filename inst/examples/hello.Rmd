---
title: "Tutor"
output: 
  html_document:
    theme: united
runtime: shiny_prerendered
---

```{r setup, include=FALSE}
library(tutor)
```

```{r, interactive=TRUE}
library(ggplot2)
qplot(mpg, wt, data = mtcars)
```


```{r, interactive=TRUE}
library(dygraphs)
dygraph(ldeaths) %>%
  dyRangeSelector(height = 20)
```





```{r cars-setup}
cat("Setup!\n")
knitr::all_labels(label == "cars-exercise")
```

```{r cars-exercise}
summary(cars)
```

```{r cars-check}
cat("Check!")
```


