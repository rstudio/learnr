% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/question_methods.R
\name{question_initialize_ui}
\alias{question_initialize_ui}
\alias{question_completed_ui}
\alias{question_is_valid}
\alias{question_is_correct}
\alias{question_try_again_ui}
\title{Custom question methods.}
\usage{
question_initialize_ui(question, input_answer, ...)

question_completed_ui(question, input_answer, ...)

question_is_valid(question, input_answer, ...)

question_is_correct(question, input_answer, ...)

question_try_again_ui(question, input_answer, ...)
}
\arguments{
\item{question}{\link{question} object used}

\item{input_answer}{user input value}

\item{...}{future parameter expansion and custom arguments to be used in dispatched s3 methods.}
}
\description{
There are five methods used to define a custom question.  Each S3 method
should correspond to the \code{type = TYPE} supplied to the question.
\itemize{
\item \code{question_initialize_ui.TYPE(question, input_answer, ...)}
\itemize{
\item Determines how the question is initially displayed to the users. This should return a shiny UI object that can be displayed using \link[shiny:renderUI]{shiny::renderUI}. For example, in the case of \code{question_initialize_ui.radio}, it returns a \link[shiny:radioButtons]{shiny::radioButtons} object. This method will be re-executed if the question is attempted again.
}
\item \code{question_completed_ui.TYPE(question, ...)}
\itemize{
\item Determines how the question is displayed after a submission.  Just like \code{question_initialize_ui}, this method should return an shiny UI object that can be displayed using \link[shiny:renderUI]{shiny::renderUI}.
}
\item \code{question_is_valid.TYPE(question, input_answer, ...)}
\itemize{
\item This method should return a boolean that determines if the input answer is valid.  Depending on the value, this function enables and disables the submission button.
}
\item \code{question_is_correct.TYPE(question, input_answer, ...)}
\itemize{
\item This function should return the output of \link{question_is_correct_value}. \link{question_is_correct_value} allows for custom messages in addition to the determination of an answer being correct.  See \link{question_is_correct_value} for more details.
}
\item \code{question_try_again_ui <- function(question, input_answer, ...)}
\itemize{
\item Determines how the question is displayed to the users while  the "Try again" screen is displayed.  Usually this function will disable inputs to the question, i.e. prevent the student from changing the answer options. Similar to \code{question_initialize_ui}, this should should return a shiny UI object that can be displayed using \link[shiny:renderUI]{shiny::renderUI}.
}
}
}
\seealso{
For more information and question type extension examples, please view the \code{question_type} tutorial: \code{learnr::run_tutorial("question_type", "learnr")}.
}
